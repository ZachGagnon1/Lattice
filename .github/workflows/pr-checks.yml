name: PR Checks

on:
  pull_request:
    branches:
      - main
      - master

jobs:
  validate:
    name: Build & Format Check
    runs-on: ubuntu-latest

    steps:
      - name: 🛑 Checkout Repository
        uses: actions/checkout@v7

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10 # Adjust to match your local pnpm version if needed

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24' # Node 20 complies with MJML 5 CI constraints
          cache: 'pnpm'

      - name: 📥 Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: 💅 Check Formatting (Prettier)
        # Validates formatting without attempting to overwrite files in CI
        run: pnpm exec prettier --check "**/*.{ts,tsx,js,jsx,json,md,scss}"

      - name: 🏗️ Build Packages
        # Executes the build script recursively across your workspace
        run: pnpm run --recursive build

      - name: 🔎 Type-check the Library
        # The build skips the test files, so this is the only check of the type tests in *.test-d.ts.
        run: pnpm --dir packages/lattice exec tsc --noEmit -p tsconfig.json

      - name: 🧪 Run the Library Tests
        run: pnpm --dir packages/lattice test
